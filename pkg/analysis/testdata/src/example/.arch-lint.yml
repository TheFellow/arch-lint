specs:
  - name: clean architecture
    packages:
      include:
        - "domain/**"
    rules:
      forbid:
        - "**"

  - name: controllers without infrastructure
    packages:
      include:
        - "controllers/**"
    rules:
      forbid:
        - "infrastructure/**"
